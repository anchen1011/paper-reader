# paper-reader

Private workspace for building and experimenting with a paper reading tool.

## Project layout

- `src/` - application code
- `tests/` - test suite
- `docs/` - notes, ideas, and design docs

## Getting started

1. Add implementation code to `src/`
2. Add tests to `tests/`
3. Capture design notes in `docs/`
