[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "paper-reader-source"
version = "0.1.0"
description = "Source collectors for paper-reader"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
